Lambda function to compress images in S3. Based on http://docs.aws.amazon.com/lambda/latest/dg/with-s3.html.
Install with yarn install (or npm install if you prefer), zip (zip -r index.zip *) & upload to lambda.
